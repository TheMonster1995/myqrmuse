import React from 'react';
import { ClipLoader } from 'react-spinners';

const ButtonLoadingSpinner: React.FC = () => (
  <ClipLoader color="#ecf0f1" size={20} />
);

export default ButtonLoadingSpinner;
